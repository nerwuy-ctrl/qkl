# app.py 与 index.html 逐行代码注释说明

本文档逐行解释后端 `app.py` 与前端 `static/index.html` 的作用和实现方式。说明不会改动源代码；同一源码行中包含多个压缩书写的 CSS、HTML 或 JavaScript 语句时，会按该行整体逻辑解释。

> 阅读提示：Python 依靠缩进表示代码块；HTML 构建页面结构；CSS 控制视觉与布局；JavaScript 负责接口请求和动态交互。

## 一、后端 app.py

源文件：[app.py](app.py)，共 348 行（按文件当前内容编号）。

| 行号 | 原代码 | 作用及实现方式 |
|---:|---|---|
| 1 | <code>from __future__ import annotations</code> | 作用：推迟类型注解的求值。实现：启用 future 特性，使前向引用和复杂类型标注在运行时不会被立即解析。 |
| 2 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 3 | <code>import hashlib</code> | 作用：引入哈希算法。实现：后续调用 SHA-256 为合同数据、载荷和模拟交易生成不可逆摘要。 |
| 4 | <code>import json</code> | 作用：引入 JSON 序列化。实现：把字典按稳定格式转成字符串后参与哈希计算。 |
| 5 | <code>from datetime import date, datetime, timedelta</code> | 作用：导入日期时间工具。实现：分别处理到期日、创建时间以及演示数据的期限偏移。 |
| 6 | <code>from pathlib import Path</code> | 作用：导入面向对象路径工具。实现：用 Path 安全拼接数据库和静态页面的绝对路径。 |
| 7 | <code>from typing import Generator, Literal, Optional</code> | 作用：导入类型标注工具。实现：描述生成器、枚举字面量和可空字段，便于校验与静态检查。 |
| 8 | <code>from urllib.parse import unquote</code> | 作用：导入 URL 解码函数。实现：将请求头中百分号编码的中文身份信息还原。 |
| 9 | <code>from uuid import uuid4</code> | 作用：导入随机 UUID 生成器。实现：为资产号、贷款号和交易哈希加入高概率唯一值。 |
| 10 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 11 | <code>from fastapi import Depends, FastAPI, Header, HTTPException</code> | 作用：导入 FastAPI 核心组件。实现：用于创建应用、声明依赖与请求头，并返回标准 HTTP 异常。 |
| 12 | <code>from fastapi.responses import FileResponse</code> | 作用：导入文件响应。实现：把前端 HTML 文件直接作为首页响应发送。 |
| 13 | <code>from pydantic import BaseModel, Field</code> | 作用：导入请求数据模型与字段约束。实现：FastAPI 据此自动解析并校验 JSON 请求体。 |
| 14 | <code>from sqlalchemy import Date, DateTime, Float, ForeignKey, Integer, String, create_engine, func, select</code> | 作用：导入 SQLAlchemy 字段类型、查询和聚合工具。实现：定义表结构并构造类型安全的数据库查询。 |
| 15 | <code>from sqlalchemy.orm import DeclarativeBase, Mapped, Session, mapped_column, relationship, sessionmaker</code> | 作用：导入 SQLAlchemy ORM 组件。实现：建立声明式模型、会话、字段映射和表间关系。 |
| 16 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 17 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 18 | <code>ROOT = Path(__file__).resolve().parent</code> | 作用：确定项目根目录。实现：由当前 Python 文件的绝对路径取父目录，避免依赖启动时工作目录。 |
| 19 | <code>engine = create_engine(f"sqlite:///{ROOT / 'supply_chain.db'}", connect_args={"check_same_thread": False})</code> | 作用：创建 SQLite 数据库引擎。实现：数据库文件放在项目根目录，并关闭 SQLite 的同线程限制以适配 Web 请求。 |
| 20 | <code>SessionLocal = sessionmaker(bind=engine, expire_on_commit=False)</code> | 作用：创建会话工厂。实现：绑定数据库引擎，并让提交后的 ORM 对象仍可直接读取属性。 |
| 21 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 22 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 23 | <code>class Base(DeclarativeBase):</code> | 作用：定义全部 ORM 模型的共同基类。实现：继承 SQLAlchemy 的 DeclarativeBase 以收集表元数据。 |
| 24 | <code>    pass</code> | 作用：保持基类主体为空。实现：Python 的 pass 作为合法占位语句，实际 ORM 能力由父类提供。 |
| 25 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 26 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 27 | <code>class Receivable(Base):</code> | 作用：声明应收账款 ORM 模型。实现：继承 Base，使类属性映射到数据库字段。 |
| 28 | <code>    __tablename__ = "receivables"</code> | 作用：指定数据库表名为 "receivables"。实现：SQLAlchemy 用它把当前模型映射到对应表。 |
| 29 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 30 | <code>    id: Mapped[int] = mapped_column(primary_key=True)</code> | 作用：定义 id 主键。实现：mapped_column(primary_key=True) 创建唯一且带索引语义的记录标识。 |
| 31 | <code>    asset_id: Mapped[str] = mapped_column(String(32), unique=True, index=True)</code> | 作用：定义模型字段 asset_id。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及唯一、索引约束。 |
| 32 | <code>    supplier: Mapped[str] = mapped_column(String(80))</code> | 作用：定义模型字段 supplier。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 33 | <code>    core_enterprise: Mapped[str] = mapped_column(String(80))</code> | 作用：定义模型字段 core_enterprise。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 34 | <code>    amount: Mapped[float] = mapped_column(Float)</code> | 作用：定义模型字段 amount。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 35 | <code>    due_date: Mapped[date] = mapped_column(Date)</code> | 作用：定义模型字段 due_date。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 36 | <code>    contract_no: Mapped[str] = mapped_column(String(80))</code> | 作用：定义模型字段 contract_no。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 37 | <code>    file_hash: Mapped[str] = mapped_column(String(64))</code> | 作用：定义模型字段 file_hash。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 38 | <code>    status: Mapped[str] = mapped_column(String(24), default="PENDING")</code> | 作用：定义模型字段 status。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及带默认值约束。 |
| 39 | <code>    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)</code> | 作用：定义模型字段 created_at。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及带默认值约束。 |
| 40 | <code>    loans: Mapped[list["Loan"]] = relationship(back_populates="receivable")</code> | 作用：定义 loans 对象关系。实现：relationship 与 back_populates 建立双向 ORM 导航。 |
| 41 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 42 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 43 | <code>class Loan(Base):</code> | 作用：声明融资申请 ORM 模型。实现：通过外键和 relationship 关联对应应收账款。 |
| 44 | <code>    __tablename__ = "loans"</code> | 作用：指定数据库表名为 "loans"。实现：SQLAlchemy 用它把当前模型映射到对应表。 |
| 45 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 46 | <code>    id: Mapped[int] = mapped_column(primary_key=True)</code> | 作用：定义 id 主键。实现：mapped_column(primary_key=True) 创建唯一且带索引语义的记录标识。 |
| 47 | <code>    loan_id: Mapped[str] = mapped_column(String(32), unique=True, index=True)</code> | 作用：定义模型字段 loan_id。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及唯一、索引约束。 |
| 48 | <code>    receivable_id: Mapped[int] = mapped_column(ForeignKey("receivables.id"))</code> | 作用：定义 receivable_id 外键。实现：ForeignKey 把融资记录连接到 receivables 表的主键。 |
| 49 | <code>    loan_amount: Mapped[float] = mapped_column(Float)</code> | 作用：定义模型字段 loan_amount。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 50 | <code>    risk_score: Mapped[int] = mapped_column(Integer)</code> | 作用：定义模型字段 risk_score。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 51 | <code>    risk_level: Mapped[str] = mapped_column(String(16))</code> | 作用：定义模型字段 risk_level。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 52 | <code>    status: Mapped[str] = mapped_column(String(24), default="APPLIED")</code> | 作用：定义模型字段 status。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及带默认值约束。 |
| 53 | <code>    interest_rate: Mapped[Optional[float]] = mapped_column(Float, nullable=True)</code> | 作用：定义模型字段 interest_rate。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及可为空约束。 |
| 54 | <code>    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)</code> | 作用：定义模型字段 created_at。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及带默认值约束。 |
| 55 | <code>    receivable: Mapped[Receivable] = relationship(back_populates="loans")</code> | 作用：定义 receivable 对象关系。实现：relationship 与 back_populates 建立双向 ORM 导航。 |
| 56 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 57 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 58 | <code>class ChainEvent(Base):</code> | 作用：声明模拟链上事件模型。实现：保存参与方、载荷摘要、交易哈希与区块高度等审计数据。 |
| 59 | <code>    __tablename__ = "chain_events"</code> | 作用：指定数据库表名为 "chain_events"。实现：SQLAlchemy 用它把当前模型映射到对应表。 |
| 60 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 61 | <code>    id: Mapped[int] = mapped_column(primary_key=True)</code> | 作用：定义 id 主键。实现：mapped_column(primary_key=True) 创建唯一且带索引语义的记录标识。 |
| 62 | <code>    asset_id: Mapped[str] = mapped_column(String(32), index=True)</code> | 作用：定义模型字段 asset_id。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及索引约束。 |
| 63 | <code>    event_type: Mapped[str] = mapped_column(String(40))</code> | 作用：定义模型字段 event_type。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 64 | <code>    actor: Mapped[str] = mapped_column(String(80))</code> | 作用：定义模型字段 actor。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 65 | <code>    role: Mapped[str] = mapped_column(String(30))</code> | 作用：定义模型字段 role。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 66 | <code>    payload_hash: Mapped[str] = mapped_column(String(64))</code> | 作用：定义模型字段 payload_hash。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 67 | <code>    tx_hash: Mapped[str] = mapped_column(String(66), unique=True)</code> | 作用：定义模型字段 tx_hash。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及唯一约束。 |
| 68 | <code>    block_height: Mapped[int] = mapped_column(Integer)</code> | 作用：定义模型字段 block_height。实现：Mapped 提供类型标注，mapped_column 指定数据库类型和持久化映射。 |
| 69 | <code>    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)</code> | 作用：定义模型字段 created_at。实现：Mapped 提供类型标注，mapped_column 指定数据库类型及带默认值约束。 |
| 70 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 71 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 72 | <code>Base.metadata.create_all(engine)</code> | 作用：初始化数据库表。实现：应用启动时根据 ORM 元数据创建尚不存在的表，不会重复创建已有表。 |
| 73 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 74 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 75 | <code>def db_session() -&gt; Generator[Session, None, None]:</code> | 作用：定义数据库会话依赖。实现：生成器为每个请求提供 Session，并保证请求结束后关闭连接。 |
| 76 | <code>    db = SessionLocal()</code> | 作用：创建当前操作使用的数据库会话。实现：调用已绑定引擎的会话工厂。 |
| 77 | <code>    try:</code> | 作用：开始异常安全的操作区。实现：配合 finally 或 except，确保清理资源或处理失败。 |
| 78 | <code>        yield db</code> | 作用：把数据库会话交给 FastAPI 接口。实现：依赖生成器在 yield 处暂停，请求完成后继续执行清理逻辑。 |
| 79 | <code>    finally:</code> | 作用：声明无论成功失败都执行的清理块。实现：Python 异常机制保证资源最终释放。 |
| 80 | <code>        db.close()</code> | 作用：关闭数据库会话。实现：归还连接并释放当前会话占用的资源。 |
| 81 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 82 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 83 | <code>def identity(</code> | 作用：定义当前身份解析依赖。实现：从 X-Role 与 X-Actor 请求头读取角色和机构名称。 |
| 84 | <code>    x_role: str = Header(default="供应商"),</code> | 作用：声明角色请求头及默认值。实现：FastAPI 的 Header 自动完成请求头注入。 |
| 85 | <code>    x_actor: str = Header(default="华星电子"),</code> | 作用：声明操作方请求头及默认值。实现：未提供请求头时使用示例供应商名称。 |
| 86 | <code>) -&gt; tuple[str, str]:</code> | 作用：结束多行函数参数并声明返回类型。实现：tuple[str, str] 表示返回角色和操作方两个字符串。 |
| 87 | <code>    return unquote(x_role), unquote(x_actor)</code> | 作用：返回解码后的身份信息。实现：unquote 还原前端 encodeURIComponent 产生的百分号编码。 |
| 88 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 89 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 90 | <code>def require(identity_data: tuple[str, str], *roles: str) -&gt; tuple[str, str]:</code> | 作用：定义角色权限检查。实现：接收允许角色列表，不匹配时中断请求。 |
| 91 | <code>    if identity_data[0] not in roles:</code> | 作用：判断当前角色是否无权操作。实现：使用成员运算检查角色是否在允许集合中。 |
| 92 | <code>        raise HTTPException(403, f"当前角色“{identity_data[0]}”无权执行此操作")</code> | 作用：拒绝越权请求。实现：抛出 HTTP 403 异常，FastAPI 将其转换为 JSON 错误响应。 |
| 93 | <code>    return identity_data</code> | 作用：返回已通过校验的身份。实现：让调用方继续解包角色与操作方。 |
| 94 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 95 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 96 | <code>def chain_write(db: Session, asset_id: str, event_type: str, actor: str, role: str, payload: dict) -&gt; ChainEvent:</code> | 作用：封装模拟链上写入。实现：对业务载荷生成摘要，分配区块高度和交易哈希，再创建事件记录。 |
| 97 | <code>    payload_text = json.dumps(payload, ensure_ascii=False, sort_keys=True, default=str)</code> | 作用：把载荷转换为稳定文本。实现：JSON 键排序、保留中文并兼容日期等非原生对象。 |
| 98 | <code>    payload_hash = hashlib.sha256(payload_text.encode()).hexdigest()</code> | 作用：生成载荷摘要。实现：将 UTF-8 字节输入 SHA-256，得到 64 位十六进制哈希。 |
| 99 | <code>    block_height = (db.scalar(select(func.max(ChainEvent.block_height))) or 102400) + 1</code> | 作用：生成下一个模拟区块高度。实现：查询当前最大高度；无数据时以 102400 为基数再加一。 |
| 100 | <code>    tx_hash = "0x" + hashlib.sha256(f"{asset_id}{event_type}{actor}{datetime.now().isoformat()}{uuid4()}".encode()).hexdigest()</code> | 作用：生成唯一交易哈希。实现：组合业务信息、当前时间和 UUID 后做 SHA-256，并添加 0x 前缀。 |
| 101 | <code>    event = ChainEvent(</code> | 作用：开始构造链上事件对象。实现：把下方关键字参数映射到 ChainEvent 各字段。 |
| 102 | <code>        asset_id=asset_id,</code> | 作用：为事件写入 asset_id 字段。实现：使用同名局部变量作为 ORM 构造参数。 |
| 103 | <code>        event_type=event_type,</code> | 作用：为事件写入 event_type 字段。实现：使用同名局部变量作为 ORM 构造参数。 |
| 104 | <code>        actor=actor,</code> | 作用：为事件写入 actor 字段。实现：使用同名局部变量作为 ORM 构造参数。 |
| 105 | <code>        role=role,</code> | 作用：为事件写入 role 字段。实现：使用同名局部变量作为 ORM 构造参数。 |
| 106 | <code>        payload_hash=payload_hash,</code> | 作用：为事件写入 payload_hash 字段。实现：使用同名局部变量作为 ORM 构造参数。 |
| 107 | <code>        tx_hash=tx_hash,</code> | 作用：为事件写入 tx_hash 字段。实现：使用同名局部变量作为 ORM 构造参数。 |
| 108 | <code>        block_height=block_height,</code> | 作用：为事件写入 block_height 字段。实现：使用同名局部变量作为 ORM 构造参数。 |
| 109 | <code>    )</code> | 作用：结束当前多行函数调用或参数列表。实现：闭合前方左括号，使表达式完整。 |
| 110 | <code>    db.add(event)</code> | 作用：把事件加入数据库会话。实现：先进入待持久化状态，随后由调用方统一 commit。 |
| 111 | <code>    return event</code> | 作用：返回新建事件对象。实现：便于调用方继续读取或扩展该事件。 |
| 112 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 113 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 114 | <code>def risk_for(receivable: Receivable, amount: float) -&gt; tuple[int, str, list[str]]:</code> | 作用：计算融资风险分数、等级和原因。实现：综合账期、融资比例与确权状态进行规则扣分。 |
| 115 | <code>    days = (receivable.due_date - date.today()).days</code> | 作用：计算距到期日的剩余天数。实现：日期相减得到 timedelta，再读取 days。 |
| 116 | <code>    ratio = amount / receivable.amount</code> | 作用：计算融资比例。实现：融资金额除以应收账款总额。 |
| 117 | <code>    score = 88</code> | 作用：设置风险评分初始值 88。实现：后续命中风险规则时逐项扣分。 |
| 118 | <code>    reasons: list[str] = []</code> | 作用：初始化风险原因列表。实现：使用带元素类型标注的空列表收集命中规则。 |
| 119 | <code>    if days &lt; 30:</code> | 作用：识别不足 30 天的短账期。实现：满足条件时进入最高期限风险扣分分支。 |
| 120 | <code>        score -= 18</code> | 作用：按当前命中的规则扣减风险分。实现：使用 -= 将 18 从现有分数中减去。 |
| 121 | <code>        reasons.append("距到期日不足30天")</code> | 作用：记录本次扣分原因。实现：向 reasons 列表追加说明文本 "距到期日不足30天"。 |
| 122 | <code>    elif days &lt; 90:</code> | 作用：识别 30—89 天的较短账期。实现：仅在前一个条件不满足时执行较轻扣分。 |
| 123 | <code>        score -= 8</code> | 作用：按当前命中的规则扣减风险分。实现：使用 -= 将 8 从现有分数中减去。 |
| 124 | <code>        reasons.append("账款剩余期限较短")</code> | 作用：记录本次扣分原因。实现：向 reasons 列表追加说明文本 "账款剩余期限较短"。 |
| 125 | <code>    if ratio &gt; 0.8:</code> | 作用：识别融资比例超过 80%。实现：高比例占用触发较大扣分。 |
| 126 | <code>        score -= 16</code> | 作用：按当前命中的规则扣减风险分。实现：使用 -= 将 16 从现有分数中减去。 |
| 127 | <code>        reasons.append("融资比例超过账款金额的80%")</code> | 作用：记录本次扣分原因。实现：向 reasons 列表追加说明文本 "融资比例超过账款金额的80%"。 |
| 128 | <code>    elif ratio &gt; 0.6:</code> | 作用：识别融资比例为 60%—80%。实现：使用 elif 避免与高风险比例重复扣分。 |
| 129 | <code>        score -= 7</code> | 作用：按当前命中的规则扣减风险分。实现：使用 -= 将 7 从现有分数中减去。 |
| 130 | <code>        reasons.append("融资比例偏高")</code> | 作用：记录本次扣分原因。实现：向 reasons 列表追加说明文本 "融资比例偏高"。 |
| 131 | <code>    if receivable.status != "CONFIRMED":</code> | 作用：检查账款是否已确权。实现：未处于 CONFIRMED 状态时施加显著扣分并记录原因。 |
| 132 | <code>        score -= 30</code> | 作用：按当前命中的规则扣减风险分。实现：使用 -= 将 30 从现有分数中减去。 |
| 133 | <code>        reasons.append("账款尚未完成核心企业确权")</code> | 作用：记录本次扣分原因。实现：向 reasons 列表追加说明文本 "账款尚未完成核心企业确权"。 |
| 134 | <code>    score = max(0, min(100, score))</code> | 作用：限制评分边界。实现：先取不低于 0，再取不高于 100，防止规则计算越界。 |
| 135 | <code>    level = "低风险" if score &gt;= 75 else "中风险" if score &gt;= 55 else "高风险"</code> | 作用：把数值评分转换为风险等级。实现：用条件表达式按 75 和 55 两个阈值划分低、中、高风险。 |
| 136 | <code>    return score, level, reasons or ["账款期限与融资比例均处于合理区间"]</code> | 作用：返回完整风控结果。实现：无扣分原因时提供一条默认的合理区间说明。 |
| 137 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 138 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 139 | <code>def receivable_out(row: Receivable) -&gt; dict:</code> | 作用：把应收账款 ORM 对象转为接口字典。实现：显式选择对外字段，避免返回内部关系或会话状态。 |
| 140 | <code>    return {</code> | 作用：开始构造返回字典。实现：下方键值对将模型字段组织为可 JSON 序列化的数据。 |
| 141 | <code>        "asset_id": row.asset_id,</code> | 作用：设置返回数据的 asset_id 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 142 | <code>        "supplier": row.supplier,</code> | 作用：设置返回数据的 supplier 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 143 | <code>        "core_enterprise": row.core_enterprise,</code> | 作用：设置返回数据的 core_enterprise 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 144 | <code>        "amount": row.amount,</code> | 作用：设置返回数据的 amount 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 145 | <code>        "due_date": row.due_date,</code> | 作用：设置返回数据的 due_date 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 146 | <code>        "contract_no": row.contract_no,</code> | 作用：设置返回数据的 contract_no 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 147 | <code>        "file_hash": row.file_hash,</code> | 作用：设置返回数据的 file_hash 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 148 | <code>        "status": row.status,</code> | 作用：设置返回数据的 status 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 149 | <code>        "created_at": row.created_at,</code> | 作用：设置返回数据的 created_at 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 150 | <code>    }</code> | 作用：结束当前字典或代码块中的对象结构。实现：闭合前方的花括号。 |
| 151 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 152 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 153 | <code>def loan_out(row: Loan) -&gt; dict:</code> | 作用：把融资 ORM 对象转为接口字典。实现：同时读取关联账款信息，拼装前端需要的结构。 |
| 154 | <code>    return {</code> | 作用：开始构造返回字典。实现：下方键值对将模型字段组织为可 JSON 序列化的数据。 |
| 155 | <code>        "loan_id": row.loan_id,</code> | 作用：设置返回数据的 loan_id 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 156 | <code>        "asset_id": row.receivable.asset_id,</code> | 作用：设置返回数据的 asset_id 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 157 | <code>        "supplier": row.receivable.supplier,</code> | 作用：设置返回数据的 supplier 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 158 | <code>        "loan_amount": row.loan_amount,</code> | 作用：设置返回数据的 loan_amount 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 159 | <code>        "risk_score": row.risk_score,</code> | 作用：设置返回数据的 risk_score 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 160 | <code>        "risk_level": row.risk_level,</code> | 作用：设置返回数据的 risk_level 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 161 | <code>        "status": row.status,</code> | 作用：设置返回数据的 status 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 162 | <code>        "interest_rate": row.interest_rate,</code> | 作用：设置返回数据的 interest_rate 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 163 | <code>        "created_at": row.created_at,</code> | 作用：设置返回数据的 created_at 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 164 | <code>    }</code> | 作用：结束当前字典或代码块中的对象结构。实现：闭合前方的花括号。 |
| 165 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 166 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 167 | <code>class ReceivableCreate(BaseModel):</code> | 作用：声明应收账款 ORM 模型。实现：继承 Base，使类属性映射到数据库字段。 |
| 168 | <code>    supplier: str = Field(min_length=2, max_length=80)</code> | 作用：定义供应商名称规则。实现：要求字符串长度在 2—80 个字符之间。 |
| 169 | <code>    core_enterprise: str = Field(min_length=2, max_length=80)</code> | 作用：定义核心企业名称规则。实现：要求字符串长度在 2—80 个字符之间。 |
| 170 | <code>    amount: float = Field(gt=0)</code> | 作用：定义账款金额规则。实现：Pydantic Field(gt=0) 要求金额严格大于零。 |
| 171 | <code>    due_date: date</code> | 作用：定义到期日字段。实现：Pydantic 将 ISO 日期字符串解析为 date 对象。 |
| 172 | <code>    contract_no: str = Field(min_length=2, max_length=80)</code> | 作用：定义合同编号规则。实现：限制为 2—80 个字符。 |
| 173 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 174 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 175 | <code>class ConfirmRequest(BaseModel):</code> | 作用：声明确权请求模型。实现：Literal 将结果限制为确认或驳回两种固定值。 |
| 176 | <code>    result: Literal["CONFIRMED", "REJECTED"]</code> | 作用：限制请求结果取值。实现：Literal 使不在列举范围内的值直接校验失败。 |
| 177 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 178 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 179 | <code>class LoanCreate(BaseModel):</code> | 作用：声明融资申请 ORM 模型。实现：通过外键和 relationship 关联对应应收账款。 |
| 180 | <code>    asset_id: str</code> | 作用：声明关联账款编号。实现：请求解析时要求该字段为字符串。 |
| 181 | <code>    loan_amount: float = Field(gt=0)</code> | 作用：声明融资金额规则。实现：Field(gt=0) 拒绝零或负数。 |
| 182 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 183 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 184 | <code>class ApprovalRequest(BaseModel):</code> | 作用：声明融资审批请求模型。实现：约束审批结果，并允许填写 0%—30% 的可选利率。 |
| 185 | <code>    result: Literal["APPROVED", "REJECTED"]</code> | 作用：限制请求结果取值。实现：Literal 使不在列举范围内的值直接校验失败。 |
| 186 | <code>    interest_rate: Optional[float] = Field(default=None, ge=0, le=30)</code> | 作用：声明可选融资利率。实现：允许为空，非空时必须位于 0—30 之间。 |
| 187 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 188 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 189 | <code>app = FastAPI(title="供应链金融平台", version="1.0.0")</code> | 作用：创建 FastAPI 应用实例。实现：设置 OpenAPI 文档中展示的应用名称和版本。 |
| 190 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 191 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 192 | <code>@app.get("/", include_in_schema=False)</code> | 作用：注册首页 GET 路由。实现：装饰器把根路径绑定到下方函数，并从接口文档隐藏。 |
| 193 | <code>def home() -&gt; FileResponse:</code> | 作用：定义首页处理函数。实现：返回类型标注为 FileResponse。 |
| 194 | <code>    return FileResponse(ROOT / "static" / "index.html")</code> | 作用：返回前端页面文件。实现：把项目根目录下 static/index.html 作为 HTTP 文件响应。 |
| 195 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 196 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 197 | <code>@app.get("/api/dashboard")</code> | 作用：注册 GET 接口 /api/dashboard。实现：FastAPI 装饰器将路径参数和依赖注入到下方处理函数。 |
| 198 | <code>def dashboard(db: Session = Depends(db_session)) -&gt; dict:</code> | 作用：提供仪表盘聚合数据。实现：注入数据库会话并返回指标、账款、融资和事件列表。 |
| 199 | <code>    receivables = db.scalars(select(Receivable).order_by(Receivable.created_at.desc())).all()</code> | 作用：查询全部账款并按创建时间倒序排列。实现：select + order_by 构造 SQL，scalars 提取 ORM 对象列表。 |
| 200 | <code>    loans = db.scalars(select(Loan).order_by(Loan.created_at.desc())).all()</code> | 作用：查询全部融资申请并按创建时间倒序排列。实现：执行 ORM select 后获取对象列表。 |
| 201 | <code>    events = db.scalars(select(ChainEvent).order_by(ChainEvent.block_height.desc()).limit(8)).all()</code> | 作用：查询链上事件。实现：按区块高度排序；仪表盘场景限制最近 8 条，审计场景筛选当前资产。 |
| 202 | <code>    return {</code> | 作用：开始构造返回字典。实现：下方键值对将模型字段组织为可 JSON 序列化的数据。 |
| 203 | <code>        "metrics": {</code> | 作用：设置返回数据的 metrics 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 204 | <code>            "receivable_count": len(receivables),</code> | 作用：设置返回数据的 receivable_count 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 205 | <code>            "receivable_amount": round(sum(x.amount for x in receivables), 2),</code> | 作用：设置返回数据的 receivable_amount 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 206 | <code>            "loan_amount": round(sum(x.loan_amount for x in loans if x.status != "REJECTED"), 2),</code> | 作用：设置返回数据的 loan_amount 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 207 | <code>            "chain_events": db.scalar(select(func.count(ChainEvent.id))) or 0,</code> | 作用：设置返回数据的 chain_events 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 208 | <code>        },</code> | 作用：结束 metrics 嵌套字典并保留外层后续字段。实现：右花括号闭合指标对象，逗号分隔下一个顶层键值对。 |
| 209 | <code>        "receivables": [receivable_out(x) for x in receivables],</code> | 作用：设置返回数据的 receivables 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 210 | <code>        "loans": [loan_out(x) for x in loans],</code> | 作用：设置返回数据的 loans 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 211 | <code>        "events": [event_dict(x) for x in events],</code> | 作用：设置返回数据的 events 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 212 | <code>    }</code> | 作用：结束当前字典或代码块中的对象结构。实现：闭合前方的花括号。 |
| 213 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 214 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 215 | <code>@app.post("/api/receivables")</code> | 作用：注册 POST 接口 /api/receivables。实现：FastAPI 装饰器将请求体、路径参数和依赖交给下方处理函数。 |
| 216 | <code>def create_receivable(</code> | 作用：处理新增应收账款。实现：校验请求与权限，生成编号和摘要，再落库并写链上事件。 |
| 217 | <code>    body: ReceivableCreate,</code> | 作用：声明接口的路径参数或请求体参数。实现：FastAPI 根据类型标注自动取值、解析和校验。 |
| 218 | <code>    who: tuple[str, str] = Depends(identity),</code> | 作用：注入当前用户身份。实现：FastAPI 先执行 identity 依赖，再把角色与操作方元组传入接口。 |
| 219 | <code>    db: Session = Depends(db_session),</code> | 作用：注入数据库会话。实现：FastAPI 管理 db_session 生成器的创建与关闭生命周期。 |
| 220 | <code>) -&gt; dict:</code> | 作用：结束新增账款接口的多行参数列表并声明返回字典。实现：闭合圆括号，返回类型标注供框架与编辑器识别。 |
| 221 | <code>    role, actor = require(who, "供应商", "管理员")</code> | 作用：执行角色授权并拆分身份。实现：require 校验允许角色，通过后返回角色和操作方。 |
| 222 | <code>    if body.due_date &lt;= date.today():</code> | 作用：拒绝已经到期或当天到期的账款。实现：将请求日期与今天比较，不合法时返回 400。 |
| 223 | <code>        raise HTTPException(400, "到期日必须晚于今天")</code> | 作用：拒绝业务规则不合法的请求。实现：抛出 HTTP 400，并把中文原因放入 detail。 |
| 224 | <code>    asset_id = "AR" + datetime.now().strftime("%Y%m%d") + uuid4().hex[:4].upper()</code> | 作用：生成应收账款业务编号。实现：拼接 AR、当前日期和 UUID 前四位大写字符。 |
| 225 | <code>    file_hash = hashlib.sha256(f"{body.contract_no}&#124;{body.supplier}&#124;{body.amount}".encode()).hexdigest()</code> | 作用：生成合同指纹。实现：组合合同号、供应商和金额后进行 SHA-256 哈希。 |
| 226 | <code>    row = Receivable(asset_id=asset_id, file_hash=file_hash, status="PENDING", **body.model_dump())</code> | 作用：构造应收账款记录。实现：写入生成字段，并通过 **body.model_dump() 展开已校验请求数据。 |
| 227 | <code>    db.add(row)</code> | 作用：将新业务对象加入会话。实现：对象进入待插入或待持久化状态。 |
| 228 | <code>    chain_write(db, asset_id, "RECEIVABLE_REGISTERED", actor, role, body.model_dump())</code> | 作用：同步记录业务事件。实现：调用统一链写入函数，保存事件类型、身份和载荷摘要。 |
| 229 | <code>    db.commit()</code> | 作用：提交当前事务。实现：把业务记录和链上事件原子性写入 SQLite。 |
| 230 | <code>    return receivable_out(row)</code> | 作用：返回最新账款数据。实现：通过输出转换函数生成前端需要的字典。 |
| 231 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 232 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 233 | <code>@app.post("/api/receivables/{asset_id}/confirm")</code> | 作用：注册 POST 接口 /api/receivables/{asset_id}/confirm。实现：FastAPI 装饰器将请求体、路径参数和依赖交给下方处理函数。 |
| 234 | <code>def confirm_receivable(</code> | 作用：处理账款确权或驳回。实现：校验角色、归属和记录存在性后更新状态。 |
| 235 | <code>    asset_id: str,</code> | 作用：声明接口的路径参数或请求体参数。实现：FastAPI 根据类型标注自动取值、解析和校验。 |
| 236 | <code>    body: ConfirmRequest,</code> | 作用：声明接口的路径参数或请求体参数。实现：FastAPI 根据类型标注自动取值、解析和校验。 |
| 237 | <code>    who: tuple[str, str] = Depends(identity),</code> | 作用：注入当前用户身份。实现：FastAPI 先执行 identity 依赖，再把角色与操作方元组传入接口。 |
| 238 | <code>    db: Session = Depends(db_session),</code> | 作用：注入数据库会话。实现：FastAPI 管理 db_session 生成器的创建与关闭生命周期。 |
| 239 | <code>) -&gt; dict:</code> | 作用：结束确权接口的多行参数列表并声明返回字典。实现：闭合圆括号后用类型注解描述响应结构。 |
| 240 | <code>    role, actor = require(who, "核心企业", "管理员")</code> | 作用：执行角色授权并拆分身份。实现：require 校验允许角色，通过后返回角色和操作方。 |
| 241 | <code>    row = db.scalar(select(Receivable).where(Receivable.asset_id == asset_id))</code> | 作用：按业务编号查询单条 ORM 记录。实现：select().where() 生成条件查询，scalar 返回首个标量结果或 None。 |
| 242 | <code>    if not row:</code> | 作用：检查查询是否没有结果。实现：ORM 对象不存在时进入错误处理分支。 |
| 243 | <code>        raise HTTPException(404, "未找到该应收账款")</code> | 作用：处理目标记录不存在。实现：抛出 HTTP 404，让前端得到明确错误原因。 |
| 244 | <code>    if role == "核心企业" and row.core_enterprise != actor:</code> | 作用：限制普通角色只能操作本机构数据。实现：同时比较当前角色与账款所属企业，不匹配则拒绝。 |
| 245 | <code>        raise HTTPException(403, "只能确权本企业相关账款")</code> | 作用：拒绝越权请求。实现：抛出 HTTP 403 异常，FastAPI 将其转换为 JSON 错误响应。 |
| 246 | <code>    row.status = body.result</code> | 作用：更新业务状态。实现：给 ORM 属性赋值，提交事务时 SQLAlchemy 自动生成 UPDATE。 |
| 247 | <code>    chain_write(db, asset_id, "RECEIVABLE_CONFIRMED" if body.result == "CONFIRMED" else "RECEIVABLE_REJECTED", actor, role, body.model_dump())</code> | 作用：同步记录业务事件。实现：调用统一链写入函数，保存事件类型、身份和载荷摘要。 |
| 248 | <code>    db.commit()</code> | 作用：提交当前事务。实现：把业务记录和链上事件原子性写入 SQLite。 |
| 249 | <code>    return receivable_out(row)</code> | 作用：返回最新账款数据。实现：通过输出转换函数生成前端需要的字典。 |
| 250 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 251 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 252 | <code>@app.post("/api/loans/apply")</code> | 作用：注册 POST 接口 /api/loans/apply。实现：FastAPI 装饰器将请求体、路径参数和依赖交给下方处理函数。 |
| 253 | <code>def apply_loan(</code> | 作用：处理融资申请。实现：检查账款、权限、金额及确权状态，计算风险并创建贷款记录。 |
| 254 | <code>    body: LoanCreate,</code> | 作用：声明接口的路径参数或请求体参数。实现：FastAPI 根据类型标注自动取值、解析和校验。 |
| 255 | <code>    who: tuple[str, str] = Depends(identity),</code> | 作用：注入当前用户身份。实现：FastAPI 先执行 identity 依赖，再把角色与操作方元组传入接口。 |
| 256 | <code>    db: Session = Depends(db_session),</code> | 作用：注入数据库会话。实现：FastAPI 管理 db_session 生成器的创建与关闭生命周期。 |
| 257 | <code>) -&gt; dict:</code> | 作用：结束融资申请接口的多行参数列表并声明返回字典。实现：闭合圆括号，冒号开始函数体。 |
| 258 | <code>    role, actor = require(who, "供应商", "管理员")</code> | 作用：执行角色授权并拆分身份。实现：require 校验允许角色，通过后返回角色和操作方。 |
| 259 | <code>    receivable = db.scalar(select(Receivable).where(Receivable.asset_id == body.asset_id))</code> | 作用：按业务编号查询单条 ORM 记录。实现：select().where() 生成条件查询，scalar 返回首个标量结果或 None。 |
| 260 | <code>    if not receivable:</code> | 作用：检查查询是否没有结果。实现：ORM 对象不存在时进入错误处理分支。 |
| 261 | <code>        raise HTTPException(404, "未找到该应收账款")</code> | 作用：处理目标记录不存在。实现：抛出 HTTP 404，让前端得到明确错误原因。 |
| 262 | <code>    if role == "供应商" and receivable.supplier != actor:</code> | 作用：限制普通角色只能操作本机构数据。实现：同时比较当前角色与账款所属企业，不匹配则拒绝。 |
| 263 | <code>        raise HTTPException(403, "只能使用本企业账款申请融资")</code> | 作用：拒绝越权请求。实现：抛出 HTTP 403 异常，FastAPI 将其转换为 JSON 错误响应。 |
| 264 | <code>    if body.loan_amount &gt; receivable.amount:</code> | 作用：执行条件判断。实现：条件为真时运行其缩进代码块，否则跳过或进入后续分支。 |
| 265 | <code>        raise HTTPException(400, "融资金额不能超过账款金额")</code> | 作用：拒绝业务规则不合法的请求。实现：抛出 HTTP 400，并把中文原因放入 detail。 |
| 266 | <code>    if receivable.status != "CONFIRMED":</code> | 作用：检查账款是否已确权。实现：未处于 CONFIRMED 状态时施加显著扣分并记录原因。 |
| 267 | <code>        raise HTTPException(400, "账款完成核心企业确权后方可融资")</code> | 作用：拒绝业务规则不合法的请求。实现：抛出 HTTP 400，并把中文原因放入 detail。 |
| 268 | <code>    score, level, reasons = risk_for(receivable, body.loan_amount)</code> | 作用：执行风控评分并拆分结果。实现：调用 risk_for 返回分数、等级和原因列表。 |
| 269 | <code>    row = Loan(</code> | 作用：开始构造融资记录。实现：下方参数写入编号、关联账款、金额和风控结果。 |
| 270 | <code>        loan_id="LN" + datetime.now().strftime("%Y%m%d") + uuid4().hex[:4].upper(),</code> | 作用：生成融资申请编号。实现：拼接 LN、当前日期和 UUID 前四位大写字符。 |
| 271 | <code>        receivable=receivable,</code> | 作用：绑定融资申请与账款对象。实现：赋值 relationship，ORM 会自动维护外键。 |
| 272 | <code>        loan_amount=body.loan_amount,</code> | 作用：写入融资或风控字段。实现：把已校验/计算的局部值传给 Loan 构造器。 |
| 273 | <code>        risk_score=score,</code> | 作用：写入融资或风控字段。实现：把已校验/计算的局部值传给 Loan 构造器。 |
| 274 | <code>        risk_level=level,</code> | 作用：写入融资或风控字段。实现：把已校验/计算的局部值传给 Loan 构造器。 |
| 275 | <code>    )</code> | 作用：结束当前多行函数调用或参数列表。实现：闭合前方左括号，使表达式完整。 |
| 276 | <code>    db.add(row)</code> | 作用：将新业务对象加入会话。实现：对象进入待插入或待持久化状态。 |
| 277 | <code>    chain_write(db, body.asset_id, "FINANCING_APPLIED", actor, role, {**body.model_dump(), "risk_score": score, "risk_level": level})</code> | 作用：同步记录业务事件。实现：调用统一链写入函数，保存事件类型、身份和载荷摘要。 |
| 278 | <code>    db.commit()</code> | 作用：提交当前事务。实现：把业务记录和链上事件原子性写入 SQLite。 |
| 279 | <code>    result = loan_out(row)</code> | 作用：生成基础融资响应。实现：先复用 loan_out，再追加本次风控原因。 |
| 280 | <code>    result["risk_reasons"] = reasons</code> | 作用：在响应中增加风险原因。实现：向结果字典写入 reasons 列表。 |
| 281 | <code>    return result</code> | 作用：返回最终接口结果。实现：FastAPI 自动把字典序列化为 JSON。 |
| 282 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 283 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 284 | <code>@app.post("/api/loans/{loan_id}/approve")</code> | 作用：注册 POST 接口 /api/loans/{loan_id}/approve。实现：FastAPI 装饰器将请求体、路径参数和依赖交给下方处理函数。 |
| 285 | <code>def approve_loan(</code> | 作用：处理融资审批。实现：仅金融机构或管理员可审批，并在通过时要求填写利率。 |
| 286 | <code>    loan_id: str,</code> | 作用：声明接口的路径参数或请求体参数。实现：FastAPI 根据类型标注自动取值、解析和校验。 |
| 287 | <code>    body: ApprovalRequest,</code> | 作用：声明接口的路径参数或请求体参数。实现：FastAPI 根据类型标注自动取值、解析和校验。 |
| 288 | <code>    who: tuple[str, str] = Depends(identity),</code> | 作用：注入当前用户身份。实现：FastAPI 先执行 identity 依赖，再把角色与操作方元组传入接口。 |
| 289 | <code>    db: Session = Depends(db_session),</code> | 作用：注入数据库会话。实现：FastAPI 管理 db_session 生成器的创建与关闭生命周期。 |
| 290 | <code>) -&gt; dict:</code> | 作用：结束融资审批接口的多行参数列表并声明返回字典。实现：闭合参数列表并以冒号进入缩进函数体。 |
| 291 | <code>    role, actor = require(who, "金融机构", "管理员")</code> | 作用：执行角色授权并拆分身份。实现：require 校验允许角色，通过后返回角色和操作方。 |
| 292 | <code>    row = db.scalar(select(Loan).where(Loan.loan_id == loan_id))</code> | 作用：按业务编号查询单条 ORM 记录。实现：select().where() 生成条件查询，scalar 返回首个标量结果或 None。 |
| 293 | <code>    if not row:</code> | 作用：检查查询是否没有结果。实现：ORM 对象不存在时进入错误处理分支。 |
| 294 | <code>        raise HTTPException(404, "未找到该融资申请")</code> | 作用：处理目标记录不存在。实现：抛出 HTTP 404，让前端得到明确错误原因。 |
| 295 | <code>    if body.result == "APPROVED" and body.interest_rate is None:</code> | 作用：校验审批通过时的必要条件。实现：结果为 APPROVED 且利率为空时返回业务错误。 |
| 296 | <code>        raise HTTPException(400, "审批通过时必须填写融资利率")</code> | 作用：拒绝业务规则不合法的请求。实现：抛出 HTTP 400，并把中文原因放入 detail。 |
| 297 | <code>    row.status = body.result</code> | 作用：更新业务状态。实现：给 ORM 属性赋值，提交事务时 SQLAlchemy 自动生成 UPDATE。 |
| 298 | <code>    row.interest_rate = body.interest_rate if body.result == "APPROVED" else None</code> | 作用：更新融资利率。实现：审批通过保存请求利率，驳回则清空为 None。 |
| 299 | <code>    chain_write(db, row.receivable.asset_id, "FINANCING_" + body.result, actor, role, body.model_dump())</code> | 作用：同步记录业务事件。实现：调用统一链写入函数，保存事件类型、身份和载荷摘要。 |
| 300 | <code>    db.commit()</code> | 作用：提交当前事务。实现：把业务记录和链上事件原子性写入 SQLite。 |
| 301 | <code>    return loan_out(row)</code> | 作用：返回最新融资数据。实现：通过输出转换函数生成稳定的接口结构。 |
| 302 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 303 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 304 | <code>def event_dict(row: ChainEvent) -&gt; dict:</code> | 作用：把链上事件 ORM 对象转为接口字典。实现：仅输出审计展示需要的字段。 |
| 305 | <code>    return {</code> | 作用：开始构造返回字典。实现：下方键值对将模型字段组织为可 JSON 序列化的数据。 |
| 306 | <code>        "event_type": row.event_type,</code> | 作用：设置返回数据的 event_type 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 307 | <code>        "asset_id": row.asset_id,</code> | 作用：设置返回数据的 asset_id 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 308 | <code>        "actor": row.actor,</code> | 作用：设置返回数据的 actor 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 309 | <code>        "role": row.role,</code> | 作用：设置返回数据的 role 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 310 | <code>        "payload_hash": row.payload_hash,</code> | 作用：设置返回数据的 payload_hash 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 311 | <code>        "tx_hash": row.tx_hash,</code> | 作用：设置返回数据的 tx_hash 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 312 | <code>        "block_height": row.block_height,</code> | 作用：设置返回数据的 block_height 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 313 | <code>        "created_at": row.created_at,</code> | 作用：设置返回数据的 created_at 字段。实现：取对象属性、计算值或下方嵌套结构作为该键的值。 |
| 314 | <code>    }</code> | 作用：结束当前字典或代码块中的对象结构。实现：闭合前方的花括号。 |
| 315 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 316 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 317 | <code>@app.get("/api/audit/{asset_id}")</code> | 作用：注册 GET 接口 /api/audit/{asset_id}。实现：FastAPI 装饰器将路径参数和依赖注入到下方处理函数。 |
| 318 | <code>def audit(asset_id: str, db: Session = Depends(db_session)) -&gt; dict:</code> | 作用：查询单笔业务的审计轨迹。实现：按资产编号读取账款与按区块高度排序的链上事件。 |
| 319 | <code>    receivable = db.scalar(select(Receivable).where(Receivable.asset_id == asset_id))</code> | 作用：按业务编号查询单条 ORM 记录。实现：select().where() 生成条件查询，scalar 返回首个标量结果或 None。 |
| 320 | <code>    if not receivable:</code> | 作用：检查查询是否没有结果。实现：ORM 对象不存在时进入错误处理分支。 |
| 321 | <code>        raise HTTPException(404, "未找到该业务编号")</code> | 作用：处理目标记录不存在。实现：抛出 HTTP 404，让前端得到明确错误原因。 |
| 322 | <code>    events = db.scalars(select(ChainEvent).where(ChainEvent.asset_id == asset_id).order_by(ChainEvent.block_height)).all()</code> | 作用：查询链上事件。实现：按区块高度排序；仪表盘场景限制最近 8 条，审计场景筛选当前资产。 |
| 323 | <code>    valid = all(e.payload_hash and len(e.payload_hash) == 64 for e in events)</code> | 作用：执行简化的链数据完整性校验。实现：确认每条事件都存在且载荷哈希长度为 64。 |
| 324 | <code>    return {"asset": receivable_out(receivable), "chain_valid": valid, "events": [event_dict(x) for x in events]}</code> | 作用：返回审计结果。实现：组合账款、校验标记和格式化后的事件列表。 |
| 325 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 326 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 327 | <code>def seed() -&gt; None:</code> | 作用：定义演示数据初始化函数。实现：数据库为空时写入示例账款和两条链上事件。 |
| 328 | <code>    with SessionLocal() as db:</code> | 作用：创建自动管理的初始化会话。实现：with 结束时自动关闭 Session。 |
| 329 | <code>        if db.scalar(select(func.count(Receivable.id))):</code> | 作用：判断数据库是否已有账款。实现：执行 count 查询，有数据便跳过重复初始化。 |
| 330 | <code>            return</code> | 作用：提前结束当前函数。实现：阻止后续演示数据被重复写入。 |
| 331 | <code>        sample = Receivable(</code> | 作用：开始构造演示账款。实现：下方固定字段组成一条已确权示例记录。 |
| 332 | <code>            asset_id="AR202606001",</code> | 作用：为事件写入 asset_id 字段。实现：使用同名局部变量作为 ORM 构造参数。 |
| 333 | <code>            supplier="华星电子",</code> | 作用：设置演示账款的 supplier 字段。实现：把固定示例值或动态计算结果传入 ORM 构造器。 |
| 334 | <code>            core_enterprise="远航科技",</code> | 作用：设置演示账款的 core_enterprise 字段。实现：把固定示例值或动态计算结果传入 ORM 构造器。 |
| 335 | <code>            amount=500000,</code> | 作用：设置演示账款的 amount 字段。实现：把固定示例值或动态计算结果传入 ORM 构造器。 |
| 336 | <code>            due_date=date.today() + timedelta(days=180),</code> | 作用：设置演示账款的 due_date 字段。实现：把固定示例值或动态计算结果传入 ORM 构造器。 |
| 337 | <code>            contract_no="HT-2026-0618",</code> | 作用：设置演示账款的 contract_no 字段。实现：把固定示例值或动态计算结果传入 ORM 构造器。 |
| 338 | <code>            file_hash=hashlib.sha256(b"demo-contract").hexdigest(),</code> | 作用：设置演示账款的 file_hash 字段。实现：把固定示例值或动态计算结果传入 ORM 构造器。 |
| 339 | <code>            status="CONFIRMED",</code> | 作用：设置演示账款的 status 字段。实现：把固定示例值或动态计算结果传入 ORM 构造器。 |
| 340 | <code>        )</code> | 作用：结束当前多行函数调用或参数列表。实现：闭合前方左括号，使表达式完整。 |
| 341 | <code>        db.add(sample)</code> | 作用：把演示账款加入会话。实现：使其与随后创建的链事件在一次事务中提交。 |
| 342 | <code>        chain_write(db, sample.asset_id, "RECEIVABLE_REGISTERED", "华星电子", "供应商", {"amount": 500000})</code> | 作用：同步记录业务事件。实现：调用统一链写入函数，保存事件类型、身份和载荷摘要。 |
| 343 | <code>        chain_write(db, sample.asset_id, "RECEIVABLE_CONFIRMED", "远航科技", "核心企业", {"result": "CONFIRMED"})</code> | 作用：同步记录业务事件。实现：调用统一链写入函数，保存事件类型、身份和载荷摘要。 |
| 344 | <code>        db.commit()</code> | 作用：提交当前事务。实现：把业务记录和链上事件原子性写入 SQLite。 |
| 345 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 346 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |
| 347 | <code>seed()</code> | 作用：在模块加载时执行演示数据初始化。实现：调用 seed，并由函数内部判断是否需要写入。 |
| 348 | （空行） | 结构分隔：用空行划分导入、模型、工具函数和接口等逻辑块，提高可读性；不参与程序执行。 |

## 二、前端 static/index.html

源文件：[static/index.html](static/index.html)，共 68 行（按文件当前内容编号）。

| 行号 | 原代码 | 作用及实现方式 |
|---:|---|---|
| 1 | <code>&lt;!doctype html&gt;</code> | 作用：声明 HTML5 文档。实现：让浏览器使用标准模式解析页面。 |
| 2 | <code>&lt;html lang="zh-CN"&gt;</code> | 作用：创建文档根元素并声明简体中文。实现：lang=zh-CN 帮助浏览器、搜索引擎和辅助技术识别语言。 |
| 3 | <code>&lt;head&gt;</code> | 作用：开始页面元数据区域。实现：其中内容配置编码、视口、标题和样式，不直接作为正文展示。 |
| 4 | <code>  &lt;meta charset="utf-8"&gt;</code> | 作用：指定 UTF-8 编码。实现：保证中文及其他 Unicode 字符被正确解析。 |
| 5 | <code>  &lt;meta name="viewport" content="width=device-width,initial-scale=1"&gt;</code> | 作用：配置移动端视口。实现：页面宽度跟随设备宽度，初始缩放为 1。 |
| 6 | <code>  &lt;title&gt;供应链金融平台&lt;/title&gt;</code> | 作用：设置浏览器标签页标题。实现：title 元素内容显示在标签页和书签中。 |
| 7 | <code>  &lt;style&gt;</code> | 作用：开始内嵌 CSS。实现：浏览器解析其中规则并应用到匹配元素。 |
| 8 | <code>    :root{--ink:#15251f;--muted:#687b73;--brand:#176b4d;--brand2:#2a946c;--paper:#f4f7f3;--line:#dce6df;--warn:#d48632}</code> | 作用：定义全局设计变量。实现：CSS 自定义属性集中保存文字、品牌、背景、边框和警示颜色。 |
| 9 | <code>    *{box-sizing:border-box}body{margin:0;background:var(--paper);color:var(--ink);font:14px/1.55 system-ui,"Microsoft YaHei",sans-serif}</code> | 作用：统一盒模型并设置页面基础外观。实现：通配选择器启用 border-box，body 设置边距、颜色、背景与系统字体。 |
| 10 | <code>    header{height:70px;background:#102d24;color:white;display:flex;align-items:center;padding:0 34px;gap:18px;box-shadow:0 8px 30px #143b2c26}</code> | 作用：设计顶部导航栏。实现：Flex 布局垂直居中，配合固定高度、深色背景、内边距和阴影。 |
| 11 | <code>    .logo{width:38px;height:38px;border:1px solid #7dd3ac;border-radius:12px;display:grid;place-items:center;font-size:20px;background:#1b5b44}.title{font-size:19px;font-weight:700}.sub{color:#9bc3b2;font-size:12px}</code> | 作用：一次定义 Logo、标题、副标题的视觉样式。实现：组合类选择器及 Grid/Flex、字号、色彩和圆角属性。 |
| 12 | <code>    .identity{margin-left:auto;display:flex;gap:9px}.identity select,.identity input{border:1px solid #396b5a;background:#183d31;color:white;border-radius:8px;padding:8px 10px}</code> | 作用：布局身份切换控件并统一输入外观。实现：margin-left:auto 将其推到右侧，子元素共享边框与配色。 |
| 13 | <code>    main{max-width:1440px;margin:auto;padding:28px 34px}.hero{display:flex;justify-content:space-between;align-items:end;margin-bottom:20px}.hero h1{font-size:26px;margin:0}.hero p{margin:5px 0 0;color:var(--muted)}</code> | 作用：设置主内容宽度、居中和留白，同时布局标题区。实现：max-width、auto 外边距与 Flex 控制页面骨架。 |
| 14 | <code>    .metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.metric,.panel{background:white;border:1px solid var(--line);border-radius:14px;box-shadow:0 5px 18px #214a3920}.metric{padding:19px}.metric b{display:block;font-size:25px;margin-top:4px}.metric span{color:var(--muted)}</code> | 作用：创建四列指标卡布局并定义卡片外观。实现：CSS Grid 均分列，卡片使用白底、边框、圆角和阴影。 |
| 15 | <code>    nav{display:flex;gap:7px;margin:22px 0 14px}button{border:0;border-radius:8px;padding:9px 14px;cursor:pointer;font-weight:600}nav button{background:#e3ebe6;color:#496158}nav button.active,.primary{background:var(--brand);color:white}.secondary{background:#e3eee8;color:var(--brand)}.danger{background:#fff0e3;color:#a85818}</code> | 作用：设置标签导航和按钮状态。实现：Flex 排列按钮，active、primary 等类控制不同语义配色。 |
| 16 | <code>    .panel{padding:20px}.view{display:none}.view.active{display:block}.split{display:grid;grid-template-columns:1.45fr .8fr;gap:15px}h2{font-size:17px;margin:0 0 15px}table{width:100%;border-collapse:collapse}th,td{border-bottom:1px solid #edf1ee;padding:11px 9px;text-align:left;white-space:nowrap}th{color:var(--muted);font-size:12px}td.money{font-weight:700}.tag{padding:3px 8px;border-radius:20px;background:#eaf5ef;color:var(--brand);font-size:12px}.tag.PENDING,.tag.APPLIED{background:#fff4df;color:#a46a15}.tag.REJECTED{background:#ffe8e3;color:#a43c2a}</code> | 作用：设置面板、视图切换、双栏布局及表格样式。实现：默认隐藏 view，仅 active 显示；Grid 创建主次两列。 |
| 17 | <code>    form{display:grid;gap:11px}label{color:var(--muted);font-size:12px}input,select{width:100%;border:1px solid #ccd9d1;border-radius:8px;padding:10px;background:white;color:var(--ink)}.actions{display:flex;gap:6px}.hash{max-width:150px;overflow:hidden;text-overflow:ellipsis;font-family:monospace;color:var(--muted)}</code> | 作用：统一表单、标签、输入框、操作区和哈希文本样式。实现：Grid 控制间距，输入框共享边框与圆角，超长哈希省略显示。 |
| 18 | <code>    .timeline{border-left:2px solid #cfe0d7;margin-left:8px;padding-left:20px}.event{position:relative;margin-bottom:18px}.event:before{content:"";position:absolute;width:10px;height:10px;border-radius:50%;background:var(--brand2);left:-26px;top:5px}.event b{display:block}.event small{color:var(--muted)}</code> | 作用：绘制审计时间线。实现：左边框作为主轴，伪元素生成事件圆点，定位属性控制节点位置。 |
| 19 | <code>    #toast{position:fixed;right:25px;bottom:25px;background:#173c30;color:white;padding:12px 18px;border-radius:10px;opacity:0;transform:translateY(10px);transition:.2s;pointer-events:none}#toast.show{opacity:1;transform:none}</code> | 作用：设计右下角消息提示及显示动画。实现：默认透明下移，添加 show 类时恢复不透明和原位。 |
| 20 | <code>    @media(max-width:900px){.metrics{grid-template-columns:1fr 1fr}.split{grid-template-columns:1fr}header{padding:0 15px}.identity input{display:none}main{padding:20px 15px}.table-wrap{overflow:auto}}</code> | 作用：适配窄屏设备。实现：900px 以下改为两列指标/单列面板，缩小边距并让表格横向滚动。 |
| 21 | <code>  &lt;/style&gt;</code> | 作用：结束内嵌 CSS。实现：关闭样式块并返回 HTML 解析。 |
| 22 | <code>&lt;/head&gt;</code> | 作用：结束元数据区域。实现：后续内容进入可见页面主体。 |
| 23 | <code>&lt;body&gt;</code> | 作用：开始可见页面主体。实现：浏览器在此构建实际界面 DOM。 |
| 24 | <code>&lt;header&gt;&lt;div class="logo"&gt;链&lt;/div&gt;&lt;div&gt;&lt;div class="title"&gt;供应链金融平台&lt;/div&gt;&lt;div class="sub"&gt;可信供应链金融协同平台&lt;/div&gt;&lt;/div&gt;&lt;div class="identity"&gt;&lt;select id="role"&gt;&lt;option&gt;供应商&lt;/option&gt;&lt;option&gt;核心企业&lt;/option&gt;&lt;option&gt;金融机构&lt;/option&gt;&lt;option&gt;监管机构&lt;/option&gt;&lt;option&gt;管理员&lt;/option&gt;&lt;/select&gt;&lt;input id="actor" value="华星电子" title="当前机构"&gt;&lt;/div&gt;&lt;/header&gt;</code> | 作用：构建顶部品牌与身份栏。实现：嵌套 Logo、标题、角色下拉框和机构输入框，并用前述 Flex 样式排列。 |
| 25 | <code>&lt;main&gt;</code> | 作用：开始主业务内容容器。实现：承载概览、指标、导航和三个功能视图。 |
| 26 | <code>  &lt;div class="hero"&gt;&lt;div&gt;&lt;h1&gt;业务总览&lt;/h1&gt;&lt;p&gt;链上凭证与链下业务协同，让每一笔融资都有迹可循。&lt;/p&gt;&lt;/div&gt;&lt;span id="clock"&gt;&lt;/span&gt;&lt;/div&gt;</code> | 作用：显示页面主标题、简介和日期。实现：hero 使用 Flex 将说明与 clock 占位元素分列对齐。 |
| 27 | <code>  &lt;section class="metrics"&gt;&lt;div class="metric"&gt;&lt;span&gt;应收账款&lt;/span&gt;&lt;b id="mCount"&gt;0&lt;/b&gt;&lt;/div&gt;&lt;div class="metric"&gt;&lt;span&gt;账款总额&lt;/span&gt;&lt;b id="mAmount"&gt;¥0&lt;/b&gt;&lt;/div&gt;&lt;div class="metric"&gt;&lt;span&gt;融资金额&lt;/span&gt;&lt;b id="mLoan"&gt;¥0&lt;/b&gt;&lt;/div&gt;&lt;div class="metric"&gt;&lt;span&gt;链上事件&lt;/span&gt;&lt;b id="mChain"&gt;0&lt;/b&gt;&lt;/div&gt;&lt;/section&gt;</code> | 作用：创建四个实时指标卡。实现：每个 b 元素具有唯一 id，JavaScript 加载数据后更新数值。 |
| 28 | <code>  &lt;nav&gt;&lt;button class="active" data-view="receivables"&gt;账款与确权&lt;/button&gt;&lt;button data-view="loans"&gt;融资审批&lt;/button&gt;&lt;button data-view="audit"&gt;链上审计&lt;/button&gt;&lt;/nav&gt;</code> | 作用：创建三项功能导航。实现：data-view 保存目标视图 id，JavaScript 点击时切换 active 类。 |
| 29 | （空行） | 结构分隔：用空行分开页面区域，便于阅读和维护；浏览器渲染时通常折叠为空白。 |
| 30 | <code>  &lt;section id="receivables" class="view active split"&gt;</code> | 作用：创建账款与确权视图。实现：左侧表格动态展示账款，右侧表单提交新账款。 |
| 31 | <code>    &lt;div class="panel"&gt;&lt;h2&gt;应收账款台账&lt;/h2&gt;&lt;div class="table-wrap"&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;业务编号&lt;/th&gt;&lt;th&gt;供应商 / 核心企业&lt;/th&gt;&lt;th&gt;金额&lt;/th&gt;&lt;th&gt;到期日&lt;/th&gt;&lt;th&gt;状态&lt;/th&gt;&lt;th&gt;操作&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody id="receivableRows"&gt;&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;&lt;/div&gt;</code> | 作用：构建应收账款表格和表头。实现：tbody 的 id 作为 JavaScript 动态插入数据行的挂载点。 |
| 32 | <code>    &lt;div class="panel"&gt;&lt;h2&gt;登记新账款&lt;/h2&gt;&lt;form id="receivableForm"&gt;&lt;div&gt;&lt;label&gt;供应商&lt;/label&gt;&lt;input name="supplier" value="华星电子" required&gt;&lt;/div&gt;&lt;div&gt;&lt;label&gt;核心企业&lt;/label&gt;&lt;input name="core_enterprise" value="远航科技" required&gt;&lt;/div&gt;&lt;div&gt;&lt;label&gt;账款金额（元）&lt;/label&gt;&lt;input name="amount" type="number" value="300000" min="1" required&gt;&lt;/div&gt;&lt;div&gt;&lt;label&gt;到期日&lt;/label&gt;&lt;input name="due_date" type="date" required&gt;&lt;/div&gt;&lt;div&gt;&lt;label&gt;合同编号&lt;/label&gt;&lt;input name="contract_no" value="HT-2026-0621" required&gt;&lt;/div&gt;&lt;button class="primary"&gt;登记并存证&lt;/button&gt;&lt;/form&gt;&lt;/div&gt;</code> | 作用：构建新增账款表单。实现：name 与后端模型字段一致，required、number、date 和 min 提供浏览器端基础校验。 |
| 33 | <code>  &lt;/section&gt;</code> | 作用：结束当前功能 section。实现：闭合对应视图容器。 |
| 34 | （空行） | 结构分隔：用空行分开页面区域，便于阅读和维护；浏览器渲染时通常折叠为空白。 |
| 35 | <code>  &lt;section id="loans" class="view split"&gt;</code> | 作用：创建融资审批视图。实现：左侧展示申请与审批按钮，右侧提交新的融资申请。 |
| 36 | <code>    &lt;div class="panel"&gt;&lt;h2&gt;融资申请列表&lt;/h2&gt;&lt;div class="table-wrap"&gt;&lt;table&gt;&lt;thead&gt;&lt;tr&gt;&lt;th&gt;申请编号&lt;/th&gt;&lt;th&gt;账款编号&lt;/th&gt;&lt;th&gt;融资金额&lt;/th&gt;&lt;th&gt;风险评分&lt;/th&gt;&lt;th&gt;状态&lt;/th&gt;&lt;th&gt;审批&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;&lt;tbody id="loanRows"&gt;&lt;/tbody&gt;&lt;/table&gt;&lt;/div&gt;&lt;/div&gt;</code> | 作用：构建融资申请表格。实现：loanRows 由 JavaScript 根据接口返回数据生成。 |
| 37 | <code>    &lt;div class="panel"&gt;&lt;h2&gt;发起融资申请&lt;/h2&gt;&lt;form id="loanForm"&gt;&lt;div&gt;&lt;label&gt;已确权账款&lt;/label&gt;&lt;select name="asset_id" id="assetSelect"&gt;&lt;/select&gt;&lt;/div&gt;&lt;div&gt;&lt;label&gt;融资金额（元）&lt;/label&gt;&lt;input name="loan_amount" type="number" value="200000" min="1" required&gt;&lt;/div&gt;&lt;button class="primary"&gt;评分并提交申请&lt;/button&gt;&lt;/form&gt;&lt;p style="color:var(--muted);margin-top:18px"&gt;系统按账龄、融资比例和确权状态形成实时风险评分，审批结果与关键证据同步写入模拟联盟链账本。&lt;/p&gt;&lt;/div&gt;</code> | 作用：构建融资申请表单和规则说明。实现：动态选择已确权账款，输入金额后调用评分申请接口。 |
| 38 | <code>  &lt;/section&gt;</code> | 作用：结束当前功能 section。实现：闭合对应视图容器。 |
| 39 | （空行） | 结构分隔：用空行分开页面区域，便于阅读和维护；浏览器渲染时通常折叠为空白。 |
| 40 | <code>  &lt;section id="audit" class="view split"&gt;</code> | 作用：创建链上审计视图。实现：左侧选择资产并显示校验摘要，右侧按时间线展示事件。 |
| 41 | <code>    &lt;div class="panel"&gt;&lt;h2&gt;业务穿透式审计&lt;/h2&gt;&lt;form id="auditForm" style="display:flex;flex-direction:row"&gt;&lt;select id="auditAsset"&gt;&lt;/select&gt;&lt;button class="primary"&gt;查询轨迹&lt;/button&gt;&lt;/form&gt;&lt;div id="auditSummary" style="margin-top:20px"&gt;&lt;/div&gt;&lt;/div&gt;</code> | 作用：构建审计查询表单与结果容器。实现：auditAsset 和 auditSummary 分别作为选择器及动态结果挂载点。 |
| 42 | <code>    &lt;div class="panel"&gt;&lt;h2&gt;链上事件时间轴&lt;/h2&gt;&lt;div class="timeline" id="timeline"&gt;&lt;/div&gt;&lt;/div&gt;</code> | 作用：构建事件时间线面板。实现：timeline 容器由脚本插入按区块排序的事件节点。 |
| 43 | <code>  &lt;/section&gt;</code> | 作用：结束当前功能 section。实现：闭合对应视图容器。 |
| 44 | <code>&lt;/main&gt;&lt;div id="toast"&gt;&lt;/div&gt;</code> | 作用：结束主内容并创建全局提示框。实现：toast 位于 body 末尾，fixed 定位使其不受页面布局影响。 |
| 45 | <code>&lt;script&gt;</code> | 作用：开始内嵌 JavaScript。实现：脚本负责接口通信、状态管理、DOM 渲染和交互事件。 |
| 46 | <code>const $=s=&gt;document.querySelector(s), fmt=n=&gt;'¥'+Number(n&#124;&#124;0).toLocaleString('zh-CN',{maximumFractionDigits:2});</code> | 作用：定义 DOM 查询和金额格式化工具。实现：$ 封装 querySelector；fmt 添加人民币符号并按中文地区格式化数字。 |
| 47 | <code>const names={PENDING:'待确权',CONFIRMED:'已确权',REJECTED:'已驳回',APPLIED:'待审批',APPROVED:'已放款'};</code> | 作用：定义后端状态码的中文映射。实现：对象以状态字符串为键，渲染时转为友好文本。 |
| 48 | <code>let state={receivables:[],loans:[]};</code> | 作用：初始化前端状态。实现：用可重新赋值对象保存账款和融资列表。 |
| 49 | <code>function headers(){return {'Content-Type':'application/json','X-Role':encodeURIComponent($('#role').value),'X-Actor':encodeURIComponent($('#actor').value)}}</code> | 作用：生成每次 API 请求的公共请求头。实现：声明 JSON 类型，并对中文角色和机构名做 URL 编码。 |
| 50 | <code>async function api(url,opt={}){let r=await fetch(url,{...opt,headers:{...headers(),...(opt.headers&#124;&#124;{})}});let data=await r.json();if(!r.ok)throw Error(data.detail&#124;&#124;'请求失败');return data}</code> | 作用：封装异步接口请求。实现：合并选项和请求头、解析 JSON，并在非成功状态时抛出带 detail 的错误。 |
| 51 | <code>function toast(msg,bad=false){let x=$('#toast');x.textContent=msg;x.style.background=bad?'#9b3e2e':'#173c30';x.classList.add('show');setTimeout(()=&gt;x.classList.remove('show'),2600)}</code> | 作用：显示成功或错误提示。实现：修改文本和背景、添加 show 类，再于 2.6 秒后移除。 |
| 52 | <code>function fd(form){let o=Object.fromEntries(new FormData(form));for(let k in o)if(['amount','loan_amount'].includes(k))o[k]=Number(o[k]);return o}</code> | 作用：把表单转换为后端 JSON 对象。实现：FormData 转键值对象，并把金额字段显式转换为数字。 |
| 53 | <code>function tag(s){return &#96;&lt;span class="tag ${s}"&gt;${names[s]&#124;&#124;s}&lt;/span&gt;&#96;}</code> | 作用：生成状态标签 HTML。实现：状态值同时作为 CSS 类，并通过 names 映射显示中文。 |
| 54 | <code>async function load(){state=await api('/api/dashboard');let m=state.metrics;$('#mCount').textContent=m.receivable_count;$('#mAmount').textContent=fmt(m.receivable_amount);$('#mLoan').textContent=fmt(m.loan_amount);$('#mChain').textContent=m.chain_events;</code> | 作用：加载并渲染整个仪表盘。实现：请求 dashboard 接口，更新指标、两张表和两个资产下拉框。 |
| 55 | <code> $('#receivableRows').innerHTML=state.receivables.map(x=&gt;&#96;&lt;tr&gt;&lt;td&gt;&lt;b&gt;${x.asset_id}&lt;/b&gt;&lt;div class="hash" title="${x.file_hash}"&gt;${x.file_hash}&lt;/div&gt;&lt;/td&gt;&lt;td&gt;${x.supplier}&lt;br&gt;&lt;small&gt;${x.core_enterprise}&lt;/small&gt;&lt;/td&gt;&lt;td class="money"&gt;${fmt(x.amount)}&lt;/td&gt;&lt;td&gt;${x.due_date}&lt;/td&gt;&lt;td&gt;${tag(x.status)}&lt;/td&gt;&lt;td class="actions"&gt;${x.status==='PENDING'?&#96;&lt;button class="secondary" onclick="confirmAsset('${x.asset_id}','CONFIRMED')"&gt;确权&lt;/button&gt;&lt;button class="danger" onclick="confirmAsset('${x.asset_id}','REJECTED')"&gt;驳回&lt;/button&gt;&#96;:'—'}&lt;/td&gt;&lt;/tr&gt;&#96;).join('');</code> | 作用：渲染应收账款表格。实现：map 把每条数据转为 HTML 行；待确权记录附带确认/驳回按钮，最后 join 拼接。 |
| 56 | <code> $('#loanRows').innerHTML=state.loans.map(x=&gt;&#96;&lt;tr&gt;&lt;td&gt;&lt;b&gt;${x.loan_id}&lt;/b&gt;&lt;/td&gt;&lt;td&gt;${x.asset_id}&lt;/td&gt;&lt;td class="money"&gt;${fmt(x.loan_amount)}&lt;/td&gt;&lt;td&gt;&lt;b&gt;${x.risk_score}&lt;/b&gt; · ${x.risk_level}&lt;/td&gt;&lt;td&gt;${tag(x.status)}&lt;/td&gt;&lt;td class="actions"&gt;${x.status==='APPLIED'?&#96;&lt;button class="secondary" onclick="approve('${x.loan_id}','APPROVED')"&gt;通过&lt;/button&gt;&lt;button class="danger" onclick="approve('${x.loan_id}','REJECTED')"&gt;拒绝&lt;/button&gt;&#96;:'—'}&lt;/td&gt;&lt;/tr&gt;&#96;).join('')&#124;&#124;'&lt;tr&gt;&lt;td colspan="6"&gt;暂无融资申请&lt;/td&gt;&lt;/tr&gt;';</code> | 作用：渲染融资申请表格。实现：map 生成数据行；待审批记录显示通过/拒绝按钮，无数据时展示占位行。 |
| 57 | <code> let options=state.receivables.filter(x=&gt;x.status==='CONFIRMED').map(x=&gt;&#96;&lt;option value="${x.asset_id}"&gt;${x.asset_id} · ${x.supplier} · ${fmt(x.amount)}&lt;/option&gt;&#96;).join('');$('#assetSelect').innerHTML=options;$('#auditAsset').innerHTML=state.receivables.map(x=&gt;&#96;&lt;option&gt;${x.asset_id}&lt;/option&gt;&#96;).join('');</code> | 作用：刷新可申请融资及可审计的资产选项。实现：筛选已确权账款生成 option，同时用全量账款填充审计下拉框。 |
| 58 | <code>}</code> | 作用：结束 load 异步函数。实现：右花括号闭合函数体，使后续确权、审批等函数成为独立的全局函数。 |
| 59 | <code>async function confirmAsset(id,result){try{await api(&#96;/api/receivables/${id}/confirm&#96;,{method:'POST',body:JSON.stringify({result})});toast('确权状态已更新并写入链上');load()}catch(e){toast(e.message,true)}}</code> | 作用：提交账款确权结果。实现：POST 结果到对应资产接口，成功提示并重新加载，失败显示错误。 |
| 60 | <code>async function approve(id,result){let interest_rate=result==='APPROVED'?Number(prompt('请输入年化融资利率（%）','4.6')):null;if(result==='APPROVED'&amp;&amp;!Number.isFinite(interest_rate))return;try{await api(&#96;/api/loans/${id}/approve&#96;,{method:'POST',body:JSON.stringify({result,interest_rate})});toast('审批结果已存证');load()}catch(e){toast(e.message,true)}}</code> | 作用：提交融资审批结果。实现：通过时弹窗取得利率并校验数字，再 POST 审批数据、提示并刷新。 |
| 61 | <code>$('#receivableForm').onsubmit=async e=&gt;{e.preventDefault();try{await api('/api/receivables',{method:'POST',body:JSON.stringify(fd(e.target))});toast('账款登记成功');load()}catch(x){toast(x.message,true)}};</code> | 作用：处理新增账款表单提交。实现：阻止浏览器刷新，序列化表单后 POST，按结果提示并重载数据。 |
| 62 | <code>$('#loanForm').onsubmit=async e=&gt;{e.preventDefault();try{let x=await api('/api/loans/apply',{method:'POST',body:JSON.stringify(fd(e.target))});toast(&#96;申请成功：风险评分 ${x.risk_score}（${x.risk_level}）&#96;);load()}catch(x){toast(x.message,true)}};</code> | 作用：处理融资申请提交。实现：阻止默认提交，发送账款和金额，成功后展示风险评分并刷新。 |
| 63 | <code>$('#auditForm').onsubmit=async e=&gt;{e.preventDefault();try{let x=await api('/api/audit/'+$('#auditAsset').value);$('#auditSummary').innerHTML=&#96;&lt;div class="metric"&gt;&lt;span&gt;凭证完整性&lt;/span&gt;&lt;b style="color:var(--brand)"&gt;${x.chain_valid?'校验通过':'校验异常'}&lt;/b&gt;&lt;small&gt;${x.asset.supplier} → ${x.asset.core_enterprise}&lt;br&gt;${fmt(x.asset.amount)} · ${names[x.asset.status]}&lt;/small&gt;&lt;/div&gt;&#96;;$('#timeline').innerHTML=x.events.map(e=&gt;&#96;&lt;div class="event"&gt;&lt;b&gt;${e.event_type}&lt;/b&gt;&lt;small&gt;区块 #${e.block_height} · ${e.actor}（${e.role}）&lt;br&gt;${new Date(e.created_at).toLocaleString()}&lt;br&gt;&lt;span class="hash" title="${e.tx_hash}"&gt;${e.tx_hash}&lt;/span&gt;&lt;/small&gt;&lt;/div&gt;&#96;).join('')}catch(x){toast(x.message,true)}};</code> | 作用：处理审计查询并渲染结果。实现：GET 选中资产，生成完整性卡片与事件时间线，失败则弹出提示。 |
| 64 | <code>document.querySelectorAll('nav button').forEach(b=&gt;b.onclick=()=&gt;{document.querySelectorAll('nav button,.view').forEach(x=&gt;x.classList.remove('active'));b.classList.add('active');$('#'+b.dataset.view).classList.add('active')});</code> | 作用：实现标签页切换。实现：为每个导航按钮绑定点击事件，先清除所有 active，再激活按钮和目标视图。 |
| 65 | <code>$('#role').onchange=()=&gt;{let map={'供应商':'华星电子','核心企业':'远航科技','金融机构':'海川银行','监管机构':'金融监管局','管理员':'平台管理员'};$('#actor').value=map[$('#role').value]};</code> | 作用：角色变化时自动切换示例机构。实现：通过角色—机构映射表更新 actor 输入框。 |
| 66 | <code>let due=new Date();due.setMonth(due.getMonth()+6);document.querySelector('[name=due_date]').value=due.toISOString().slice(0,10);$('#clock').textContent=new Date().toLocaleDateString('zh-CN',{year:'numeric',month:'long',day:'numeric'});load();</code> | 作用：完成页面初始设置并首次加载数据。实现：默认到期日设为六个月后，显示本地日期，最后调用 load。 |
| 67 | <code>&lt;/script&gt;&lt;/body&gt;&lt;/html&gt;</code> | 作用：结束脚本、页面主体和 HTML 文档。实现：依次闭合 script、body 与 html 元素。 |
| 68 | （空行） | 结构分隔：用空行分开页面区域，便于阅读和维护；浏览器渲染时通常折叠为空白。 |

## 三、整体协作关系

浏览器访问根路径时，FastAPI 返回 `static/index.html`。页面脚本通过 `/api/*` 接口读写数据；FastAPI 使用 Pydantic 校验请求、SQLAlchemy 操作 SQLite，并在关键业务动作发生时写入 `chain_events`，最终把 JSON 结果返回前端重新渲染。
