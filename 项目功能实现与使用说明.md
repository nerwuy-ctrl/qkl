# 供应链金融平台

## 项目功能实现与使用说明

### 1. 项目概述

本项目是一个可以直接运行和演示的课程项目。系统围绕供应商、核心企业、金融机构和监管机构之间的协同业务，完成应收账款登记、核心企业确权、供应商融资申请、金融机构审批、风险评分、链上存证和监管审计等功能。

项目采用“链下业务数据库 + 模拟联盟链账本”的实现方式：

- SQLite 保存应收账款、融资申请等需要频繁查询和修改的业务数据；
- `chain_events` 表以只追加、不覆盖的方式保存关键业务事件；
- SHA-256 摘要、交易哈希和区块高度用于模拟真实联盟链中的存证信息；
- FastAPI 提供后端接口并执行参数校验、权限判断和业务处理；
- HTML、CSS 和 JavaScript 构成前端管理看板，通过接口动态加载数据。

当前版本重点展示业务流程和区块链应用思路，不依赖真实区块链节点，因此适合本地运行、课堂演示和课程答辩。如果后续接入真实联盟链，只需替换链上写入函数，不需要重写主要业务流程。

---

## 2. 项目目录说明

```text
周三区块链/
├─ app.py                         后端入口、数据模型、接口和业务逻辑
├─ static/
│  └─ index.html                  前端管理看板
├─ requirements.txt               Python依赖清单
├─ supply_chain.db                首次启动后生成的SQLite数据库
├─ README.md                      简要运行说明
└─ 项目功能实现与使用说明.md       本文档
```

`app.py` 采用单文件结构，便于课程项目阅读和提交。正式生产项目可以继续拆分为 `models`、`schemas`、`services`、`blockchain` 和 `api` 等模块。

---

## 3. 技术架构

### 3.1 表现层

表现层位于 `static/index.html`，负责展示业务总览、账款台账、融资申请和审计时间轴。页面不保存业务数据，所有数据均通过 `/api` 接口获取。

页面主要使用以下技术：

- HTML：定义看板、表格、表单和操作按钮；
- CSS：实现响应式布局、状态标签、数据卡片和审计时间轴；
- JavaScript Fetch API：向 FastAPI 后端发送请求；
- DOM 操作：把接口返回的 JSON 数据动态渲染到页面。

### 3.2 业务服务层

FastAPI 是系统的业务入口，负责：

1. 接收前端提交的数据；
2. 使用 Pydantic 校验数据类型和取值范围；
3. 根据当前角色判断是否有操作权限；
4. 调用 SQLAlchemy 查询或更新数据库；
5. 计算风险评分或生成哈希；
6. 把关键业务事件写入模拟链上账本；
7. 将处理结果以 JSON 返回前端。

### 3.3 数据持久化层

项目使用 SQLAlchemy ORM 操作 SQLite。`create_engine()` 创建数据库连接，`SessionLocal` 为每次请求提供独立会话，`db_session()` 在请求结束后自动关闭连接。

首次加载程序时执行：

```python
Base.metadata.create_all(engine)
```

如果数据库不存在，SQLAlchemy 会自动创建数据库文件和数据表。程序还会执行 `seed()`，加入一条已确权的演示账款，方便首次启动后立即体验融资和审计功能。

### 3.4 区块链交互层

当前项目不要求部署真实联盟链，而是通过 `chain_write()` 模拟链上交易。每次关键操作都会：

1. 将业务参数转换为顺序稳定的 JSON；
2. 对 JSON 计算 SHA-256 摘要；
3. 根据业务编号、事件、操作人、时间和随机数生成交易哈希；
4. 在当前最大区块高度上加一；
5. 将事件追加到 `chain_events` 表。

该设计体现了区块链“关键证据上链、业务明细链下保存”的思想，并避免将大文件直接写入区块链。

---

## 4. 数据模型实现

### 4.1 应收账款表 `receivables`

该表保存供应链贸易形成的应收账款。

| 字段 | 作用 |
|---|---|
| `asset_id` | 账款业务编号，如 `AR202606001` |
| `supplier` | 供应商名称 |
| `core_enterprise` | 核心企业名称 |
| `amount` | 应收账款金额 |
| `due_date` | 到期日 |
| `contract_no` | 合同编号 |
| `file_hash` | 合同证据的SHA-256摘要 |
| `status` | `PENDING`、`CONFIRMED` 或 `REJECTED` |
| `created_at` | 登记时间 |

账款刚登记时状态为 `PENDING`，核心企业确认后变为 `CONFIRMED`，驳回后变为 `REJECTED`。只有已确权账款才能申请融资。

### 4.2 融资申请表 `loans`

该表保存供应商基于应收账款发起的融资申请。

| 字段 | 作用 |
|---|---|
| `loan_id` | 融资申请编号 |
| `receivable_id` | 关联的应收账款主键 |
| `loan_amount` | 申请融资金额 |
| `risk_score` | 系统计算的风险评分 |
| `risk_level` | 低风险、中风险或高风险 |
| `status` | `APPLIED`、`APPROVED` 或 `REJECTED` |
| `interest_rate` | 审批通过后的年化利率 |
| `created_at` | 申请时间 |

`Receivable` 与 `Loan` 通过外键和 ORM Relationship 建立一对多关系，一笔账款可以关联融资记录。

### 4.3 链上事件表 `chain_events`

该表模拟联盟链不可随意篡改的追加式账本。

| 字段 | 作用 |
|---|---|
| `asset_id` | 事件关联的账款编号 |
| `event_type` | 登记、确权、融资申请或审批等事件类型 |
| `actor` | 操作机构 |
| `role` | 操作角色 |
| `payload_hash` | 业务参数摘要 |
| `tx_hash` | 模拟链上交易哈希 |
| `block_height` | 模拟区块高度 |
| `created_at` | 上链时间 |

每次业务变化产生一条新事件，旧事件不会被更新，因此可以按区块高度恢复完整业务轨迹。

---

## 5. 身份与权限控制

### 5.1 身份传递

前端右上角提供“角色”和“当前机构”选择框。请求发送前，JavaScript 将角色和机构编码后放入请求头：

```text
X-Role: 当前角色
X-Actor: 当前机构
```

后端 `identity()` 读取并解码请求头，`require()` 检查角色是否在允许列表中。未授权操作返回 HTTP 403，前端以提示消息展示错误原因。

### 5.2 权限矩阵

| 角色 | 主要权限 |
|---|---|
| 供应商 | 登记账款；使用本企业已确权账款申请融资 |
| 核心企业 | 确认或驳回与本企业相关的账款 |
| 金融机构 | 审批融资申请并填写利率 |
| 监管机构 | 查询业务数据和链上审计轨迹 |
| 管理员 | 课堂演示所需的全部业务权限 |

系统不仅验证角色，也验证企业归属。例如，供应商不能使用其他供应商的账款融资，核心企业不能确认与自身无关的账款。

当前实现使用演示请求头代替正式登录，便于课堂切换角色。生产环境应使用账号密码、JWT Token、密码哈希和持久化用户表。

---

## 6. 功能实现详解

### 6.1 业务总览

页面启动后调用：

```http
GET /api/dashboard
```

后端统计并返回：

- 应收账款数量；
- 应收账款总金额；
- 未被拒绝的融资总金额；
- 链上事件总数；
- 最新账款、融资和链上事件列表。

前端 `load()` 方法将结果写入顶部指标卡片和各业务表格。每次登记、确权、申请或审批完成后，页面都会重新调用该接口，使看板保持最新状态。

### 6.2 应收账款登记

供应商填写供应商名称、核心企业、账款金额、到期日和合同编号，页面调用：

```http
POST /api/receivables
```

后端处理流程：

1. 验证角色必须是供应商或管理员；
2. 验证金额大于零、到期日晚于今天；
3. 生成以 `AR` 开头的唯一账款编号；
4. 使用合同编号、供应商和金额计算文件摘要；
5. 将账款以 `PENDING` 状态写入数据库；
6. 写入 `RECEIVABLE_REGISTERED` 链上事件；
7. 同时提交业务记录和链上事件，保证数据一致性。

课程演示中没有真正上传合同文件，因此使用合同关键信息生成摘要。接入文件上传后，可直接对文件二进制内容计算 SHA-256，链上仍只保存摘要。

### 6.3 核心企业确权

核心企业在待确权账款后点击“确权”或“驳回”，页面调用：

```http
POST /api/receivables/{asset_id}/confirm
```

后端首先查询账款，然后判断当前核心企业是否与账款中的核心企业一致。确权后状态变为 `CONFIRMED`，驳回后状态变为 `REJECTED`，同时产生对应链上事件。

确权的业务意义是核心企业确认贸易背景、账款金额和付款义务真实存在。金融机构只有在该步骤完成后才接受融资申请。

### 6.4 融资申请和风险评分

供应商从已确权账款中选择一笔，输入融资金额后调用：

```http
POST /api/loans/apply
```

系统依次检查：

- 账款是否存在；
- 是否属于当前供应商；
- 融资金额是否超过账款金额；
- 账款是否已完成核心企业确权。

检查通过后，`risk_for()` 根据账款剩余期限、融资金额占账款比例和确权状态计算评分。基础分为 88 分：

- 距到期日不足30天扣18分；
- 距到期日30至89天扣8分；
- 融资比例超过80%扣16分；
- 融资比例超过60%但不超过80%扣7分；
- 未确权扣30分。

评分结果限制在 0 至 100 分，并按以下规则划分风险：

| 分数 | 风险等级 |
|---|---|
| 75—100 | 低风险 |
| 55—74 | 中风险 |
| 0—54 | 高风险 |

融资申请保存后产生 `FINANCING_APPLIED` 事件，评分与风险等级也进入事件摘要，从而证明审批时使用的风险结果。

该模型属于可解释的规则评分，适合课程项目。积累真实历史数据后，可以加入付款及时率、历史违约率、三单匹配度和异常金额波动等指标，或使用 Scikit-learn 训练机器学习模型。

### 6.5 金融机构审批

金融机构进入“融资审批”页，对待审批申请点击“通过”或“拒绝”。通过时输入年化利率，页面调用：

```http
POST /api/loans/{loan_id}/approve
```

后端验证角色和融资申请是否存在。审批通过但没有填写利率时返回错误；通过后状态变为 `APPROVED`，拒绝后变为 `REJECTED`。审批结果通过 `FINANCING_APPROVED` 或 `FINANCING_REJECTED` 事件写入模拟链上账本。

### 6.6 链上审计

监管人员选择账款编号并点击“查询轨迹”，页面调用：

```http
GET /api/audit/{asset_id}
```

后端返回账款当前状态以及按区块高度升序排列的全部事件。系统同时检查每条事件是否具有格式正确的 SHA-256 摘要，并返回 `chain_valid` 校验结果。

前端将结果显示为时间轴，包含：

- 事件类型；
- 区块高度；
- 操作机构和角色；
- 操作时间；
- 交易哈希。

监管机构由此可以从账款登记开始，查看确权、融资申请和审批的完整轨迹，实现穿透式审计。

---

## 7. 接口清单

| 接口 | 方法 | 用途 |
|---|---|---|
| `/` | GET | 返回平台管理页面 |
| `/api/dashboard` | GET | 获取指标与业务列表 |
| `/api/receivables` | POST | 登记应收账款 |
| `/api/receivables/{asset_id}/confirm` | POST | 核心企业确权或驳回 |
| `/api/loans/apply` | POST | 发起融资并计算风险 |
| `/api/loans/{loan_id}/approve` | POST | 金融机构审批融资 |
| `/api/audit/{asset_id}` | GET | 查询链上历史轨迹 |
| `/docs` | GET | FastAPI自动生成的接口调试页面 |

Pydantic 会自动把接口模型加入 OpenAPI 文档。在 `/docs` 页面中可以查看字段说明、请求格式和返回结果，也可以直接发送测试请求。

---

## 8. 安装与启动

### 8.1 环境要求

- Windows 10/11；
- Python 3.9 或更高版本；
- 可用的现代浏览器。

### 8.2 首次安装

在项目目录打开 PowerShell：

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 8.3 启动系统

```powershell
.\.venv\Scripts\python.exe -m uvicorn app:app --reload --host 127.0.0.1 --port 8002
```

出现 `Uvicorn running on http://127.0.0.1:8002` 后，在浏览器访问：

```text
http://127.0.0.1:8002
```

接口文档地址：

```text
http://127.0.0.1:8002/docs
```

关闭系统时，在 PowerShell 窗口按 `Ctrl+C`。

---

## 9. 完整使用流程

### 9.1 演示一笔新业务

1. 右上角选择“供应商”，当前机构填写“华星电子”；
2. 在“登记新账款”中填写核心企业、金额、到期日和合同编号；
3. 点击“登记并存证”，账款进入待确权状态；
4. 将角色切换为“核心企业”，机构填写为账款对应的核心企业；
5. 点击账款后的“确权”；
6. 切换回“供应商”和原供应商机构；
7. 打开“融资审批”页，选择刚才的已确权账款；
8. 输入融资金额，点击“评分并提交申请”；
9. 切换为“金融机构”，点击融资申请后的“通过”；
10. 输入年化利率并确认；
11. 打开“链上审计”，选择该账款并点击“查询轨迹”；
12. 查看登记、确权、融资申请和审批对应的区块事件。

### 9.2 权限演示

可以故意使用不匹配的角色或机构执行操作，例如让其他核心企业确认“远航科技”的账款。系统会返回“只能确权本企业相关账款”，用于展示 RBAC 和数据归属控制。

### 9.3 重置演示数据

先关闭服务，然后删除项目目录中的 `supply_chain.db`，再次启动时程序会自动建立全新数据库并写入一条基础演示账款。

删除数据库会清除全部业务记录，应仅在确认不需要保留现有演示数据时操作。

---

## 10. 安全性与一致性设计

### 10.1 参数安全

Pydantic 对金额、日期、字符串长度和审批结果进行校验，错误数据不会进入业务处理。SQLAlchemy 使用参数化查询，避免直接拼接 SQL。

### 10.2 权限安全

所有写操作均在后端再次检查角色，不能仅通过修改前端按钮绕过权限。企业归属检查进一步限制数据访问范围。

### 10.3 数据一致性

业务数据更新与 `chain_events` 写入使用同一个数据库会话，并在最后统一 `commit()`。任何步骤发生异常时都不会提交，从而避免“业务成功但存证失败”或“存证成功但业务未更新”。

### 10.4 隐私与存储

合同明细和金额保存在链下数据库；模拟链上只保存业务编号、状态和摘要。该方式降低敏感数据公开风险，也减少链上存储压力。

---

## 11. 当前实现范围与后续扩展

本项目已经完成文档要求的核心业务闭环，但仍属于教学演示系统。生产化可继续扩展：

1. 增加用户表、企业认证、密码哈希和 JWT 登录；
2. 使用 MySQL 或 PostgreSQL 替换 SQLite；
3. 增加合同、发票和物流单文件上传及对象存储；
4. 接入 FISCO BCOS、Hyperledger Fabric 或以太坊联盟链；
5. 将 `chain_write()` 替换为 Web3.py 或联盟链 SDK；
6. 增加异步交易回执、失败重试和交易状态同步；
7. 增加还款、逾期、预警和贷后管理；
8. 使用真实历史数据训练风险模型；
9. 增加操作日志、数据脱敏和电子签名；
10. 使用 Vue 等前端框架拆分页面组件。

---

## 12. 总结

供应链金融平台以应收账款融资为主线，将供应商、核心企业、金融机构和监管机构连接到同一业务流程中。FastAPI 和 SQLAlchemy 负责链下业务，规则模型负责风险量化，追加式事件账本负责关键操作留痕。系统通过角色权限、企业归属、哈希摘要、交易哈希和审计时间轴，完整展示了区块链在供应链金融中“增信、确权、存证和追溯”的应用价值。

项目既可以直接作为课程功能演示，也为后续接入真实区块链节点、扩展风控模型和完善生产级安全机制保留了清晰接口。
